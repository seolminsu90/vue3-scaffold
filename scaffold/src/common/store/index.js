import { createStore } from 'vuex'

import authStore from './module/auth.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    authStore: authStore
  }
})
